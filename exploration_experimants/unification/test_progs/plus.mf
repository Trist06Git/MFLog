
//main(r) =
//  3 = r + 2.

loop(3, 3).
loop(x, r) =
  print(x),
  step = x + 1,
  print(step),
  loop(step, r).

//loop(5).
//loop(x) =
//  print(x),
//  step = x + 1,
//  loop(step).

//main(r) =
//  loop(0, r).

printnl(x) =
  print(x), nl(x).

//sort of works, fix explication.
//main(r) =
//  r = 1 + 2 + 3.

//main(r) =
//  1 = r + 3.
main(r) =
  3 = r + 1.
